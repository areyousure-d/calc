import { Button } from "@/shared/ui/button";

import styles from "../pointer-events-disabled.module.css";

export const ResultButton = () => {
  return (
    <Button className={styles["pointer-events-disabled"]}>result button</Button>
  );
};
